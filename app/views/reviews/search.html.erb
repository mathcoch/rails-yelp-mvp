<h1>Resultat de votre recherche</h1>
<ul>
  <% @reviews.each do |review| %>
    <li><%= review.content %> | <%= link_to "#{review.restaurant.name}", restaurant_path(review.restaurant) %> | <%= review.rating %></li>
  <% end %>
</ul>
